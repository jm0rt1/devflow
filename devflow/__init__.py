"""Devflow - Python-native project operations CLI.

Replace per-project shell scripts (build.sh, test.sh, publish.sh) with a single,
pip-installable Python CLI that is configurable, portable, and easy to integrate.

Usage:
    devflow --help          # Show all available commands
    devflow test            # Run tests
    devflow build           # Build distribution
    devflow publish         # Publish package

See docs/QUICKSTART.md for getting started guide.
"""

__version__ = "0.1.0"
